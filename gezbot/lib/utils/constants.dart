/// The code is importing the `material.dart` file from the `flutter` package. This file contains
/// various widgets and classes that are used to build user interfaces in Flutter.
import 'package:flutter/material.dart';

// Colors
const kBackgroundColor = Color(0xFFD2FFF4);
const kPrimaryColor = Color(0xFF2D5D70);
const kSecondaryColor = Color(0xFF265DAB);
